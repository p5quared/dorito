[project]
name = "shared"
version = "0.1.0"
description = "Shared utilities and interfaces for producer and consumer services"
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.40.11",
    "dataclasses-json>=0.6.7",
]

[build-system]
requires = ["uv_build>=0.8.12,<0.9.0"]
build-backend = "uv_build"
